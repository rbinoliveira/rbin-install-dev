# ===============================
# UNIVERSAL ZSH CONFIG (MAC + LINUX)
# ===============================

### -------------------------------
### COMPINIT (Completion System)
### -------------------------------
# Fix permissions for completion directories first
# This prevents the "insecure directories" warning on terminal startup
if [[ -d "$HOME/.zsh" ]]; then
  chmod 755 "$HOME/.zsh" 2>/dev/null || true
fi
if [[ -d "$HOME/.cache/zsh" ]]; then
  chmod 755 "$HOME/.cache/zsh" 2>/dev/null || true
fi
# Fix permissions for zcompdump files
if [[ -f "$HOME/.zcompdump" ]]; then
  chmod 600 "$HOME/.zcompdump" 2>/dev/null || true
fi
if [[ -f "$HOME/.zcompdump-"* ]]; then
  chmod 600 "$HOME/.zcompdump-"* 2>/dev/null || true
fi

# Load completion system
autoload -Uz compinit

# Initialize completion system
# Check if .zcompdump is older than 24 hours, if so regenerate it
# -C flag skips security check (safe since we fixed permissions above)
if [[ -n ${ZDOTDIR:-$HOME}/.zcompdump(#qN.mh+24) ]]; then
  compinit -C
else
  compinit -C
fi

# PATH default (Linux)
export PATH="$HOME/bin:$HOME/.local/bin:$PATH"

### -------------------------------
### EDITOR (evitar nano no git pull, etc)
### -------------------------------
export EDITOR="code --wait"
export VISUAL="code --wait"
# Git: não abrir editor em merge (evita nano ao fazer git pull com merge)
export GIT_MERGE_AUTOEDIT=no

# Add Homebrew to PATH if on macOS
if [[ "$OSTYPE" == "darwin"* ]]; then
  # Check for Apple Silicon Mac
  if [[ -d "/opt/homebrew/bin" ]]; then
    export PATH="/opt/homebrew/bin:$PATH"
  # Check for Intel Mac
  elif [[ -d "/usr/local/bin" ]]; then
    export PATH="/usr/local/bin:$PATH"
  fi
fi

# Detect OS (mac / linux)
case "$OSTYPE" in
  darwin*) export OS="mac";;
  linux*)  export OS="linux";;
esac

### -------------------------------
### ZINIT (Fast plugin manager)
### -------------------------------
if [[ ! -f ~/.zinit/bin/zinit.zsh ]]; then
  mkdir -p ~/.zinit
  git clone https://github.com/zdharma-continuum/zinit.git ~/.zinit/bin
fi

source ~/.zinit/bin/zinit.zsh

### -------------------------------
### PLUGINS (fast load)
### -------------------------------
zinit light zsh-users/zsh-autosuggestions
zinit light zsh-users/zsh-completions
zinit light zsh-users/zsh-history-substring-search

# Bind up/down arrows to history substring search
bindkey '^[[A' history-substring-search-up
bindkey '^[[B' history-substring-search-down

# Syntax highlighting MUST be last
SYNTAX_PLUGIN="zsh-users/zsh-syntax-highlighting"

### -------------------------------
### ZOXIDE (smart cd)
### -------------------------------
# Evitar conflito: zinit usa alias "zi", e algum eval pode definir função zi().
# Remove o alias antes do eval e restaura depois.
unalias zi 2>/dev/null
eval "$(zoxide init zsh)"
alias zi='zinit' 2>/dev/null

### -------------------------------
### FZF (universal configs)
### -------------------------------
export FZF_DEFAULT_COMMAND='fd --type f'
export FZF_CTRL_T_COMMAND="$FZF_DEFAULT_COMMAND"
export FZF_ALT_C_COMMAND='fd --type d'

### -------------------------------
### ALIASES
### -------------------------------
alias ls='lsd'
# cat: usa bat (ou batcat no Debian/Ubuntu) se existir; senão cat normal
if command -v bat &>/dev/null; then
  alias cat='bat'
elif command -v batcat &>/dev/null; then
  alias cat='batcat'
fi
alias g='git'
alias lg='lazygit'
alias c='clear'
alias reload='source ~/.zshrc'
alias tf='bash .task-flow/scripts/task-flow.sh'

### -------------------------------
### FUNCTIONS
### -------------------------------

# Open AI agents (Claude, Cursor) in 2 tabs in current directory
agents() {
  local current_dir=$(pwd)

  # Detect terminal and open tabs accordingly
  if [[ "$TERM_PROGRAM" == "iTerm.app" ]] || [[ -n "$ITERM_SESSION_ID" ]]; then
    # iTerm2 (if running on macOS)
    if command -v osascript &> /dev/null; then
      osascript <<EOF
tell application "iTerm"
  tell current window
    # Tab 1: Claude
    create tab with default profile
    tell current session of current tab
      write text "cd '$current_dir' && echo '🤖 Claude CLI' && claude"
    end tell

    # Tab 2: Cursor
    create tab with default profile
    tell current session of current tab
      write text "cd '$current_dir' && echo '🖥️  Cursor CLI' && cursor-agent"
    end tell
  end tell
end tell
EOF
    fi
  elif [[ "$TERM_PROGRAM" == "Apple_Terminal" ]]; then
    # Terminal.app (macOS)
    if command -v osascript &> /dev/null; then
      osascript <<EOF
tell application "Terminal"
  do script "cd '$current_dir' && echo '🤖 Claude CLI' && claude"
  tell application "System Events" to keystroke "t" using command down
  do script "cd '$current_dir' && echo '🖥️  Cursor CLI' && cursor-agent" in front window
end tell
EOF
    fi
  else
    # Linux terminals
    echo "Opening AI agents in current directory: $current_dir"

    if command -v gnome-terminal &> /dev/null; then
      gnome-terminal --tab --title="Claude CLI" -- bash -c "cd '$current_dir' && echo '🤖 Claude CLI' && claude; exec zsh" \
                     --tab --title="Cursor CLI" -- bash -c "cd '$current_dir' && echo '🖥️  Cursor CLI' && cursor-agent; exec zsh"
    elif command -v konsole &> /dev/null; then
      konsole --new-tab -e zsh -c "cd '$current_dir' && echo '🤖 Claude CLI' && claude; exec zsh" \
              --new-tab -e zsh -c "cd '$current_dir' && echo '🖥️  Cursor CLI' && cursor-agent; exec zsh"
    elif command -v xterm &> /dev/null; then
      xterm -e zsh -c "cd '$current_dir' && echo '🤖 Claude CLI' && claude; exec zsh" &
      xterm -e zsh -c "cd '$current_dir' && echo '🖥️  Cursor CLI' && cursor-agent; exec zsh" &
    else
      echo "⚠️  Terminal detection failed. Please open tabs manually:"
      echo "   Tab 1: cd '$current_dir' && claude"
      echo "   Tab 2: cd '$current_dir' && cursor-agent"
    fi
  fi
}

# Open Claude Code sessions (accounts 1–3 when configured) in separate tabs
claudes() {
  local current_dir=$(pwd)
  local has_third=false
  command -v claude3 &>/dev/null && has_third=true

  if [[ "$TERM_PROGRAM" == "iTerm.app" ]] || [[ -n "$ITERM_SESSION_ID" ]]; then
    if command -v osascript &> /dev/null; then
      if [ "$has_third" = true ]; then
        osascript <<EOF
tell application "iTerm"
  tell current window
    create tab with default profile
    tell current session of current tab
      write text "cd '$current_dir' && echo '🤖 Claude conta 1' && claude"
    end tell
    create tab with default profile
    tell current session of current tab
      write text "cd '$current_dir' && echo '🤖 Claude conta 2' && claude2"
    end tell
    create tab with default profile
    tell current session of current tab
      write text "cd '$current_dir' && echo '🤖 Claude conta 3' && claude3"
    end tell
  end tell
end tell
EOF
      else
        osascript <<EOF
tell application "iTerm"
  tell current window
    create tab with default profile
    tell current session of current tab
      write text "cd '$current_dir' && echo '🤖 Claude conta 1' && claude"
    end tell
    create tab with default profile
    tell current session of current tab
      write text "cd '$current_dir' && echo '🤖 Claude conta 2' && claude2"
    end tell
  end tell
end tell
EOF
      fi
    fi
  elif [[ "$TERM_PROGRAM" == "Apple_Terminal" ]]; then
    if command -v osascript &> /dev/null; then
      if [ "$has_third" = true ]; then
        osascript <<EOF
tell application "Terminal"
  do script "cd '$current_dir' && echo '🤖 Claude conta 1' && claude"
  tell application "System Events" to keystroke "t" using command down
  do script "cd '$current_dir' && echo '🤖 Claude conta 2' && claude2" in front window
  tell application "System Events" to keystroke "t" using command down
  do script "cd '$current_dir' && echo '🤖 Claude conta 3' && claude3" in front window
end tell
EOF
      else
        osascript <<EOF
tell application "Terminal"
  do script "cd '$current_dir' && echo '🤖 Claude conta 1' && claude"
  tell application "System Events" to keystroke "t" using command down
  do script "cd '$current_dir' && echo '🤖 Claude conta 2' && claude2" in front window
end tell
EOF
      fi
    fi
  else
    echo "Opening Claude accounts in: $current_dir"
    if command -v gnome-terminal &> /dev/null; then
      if [ "$has_third" = true ]; then
        gnome-terminal --tab --title="Claude 1" -- bash -c "cd '$current_dir' && claude; exec zsh" \
                       --tab --title="Claude 2" -- bash -c "cd '$current_dir' && claude2; exec zsh" \
                       --tab --title="Claude 3" -- bash -c "cd '$current_dir' && claude3; exec zsh"
      else
        gnome-terminal --tab --title="Claude 1" -- bash -c "cd '$current_dir' && claude; exec zsh" \
                       --tab --title="Claude 2" -- bash -c "cd '$current_dir' && claude2; exec zsh"
      fi
    elif command -v konsole &> /dev/null; then
      konsole --new-tab -e zsh -c "cd '$current_dir' && claude; exec zsh" \
              --new-tab -e zsh -c "cd '$current_dir' && claude2; exec zsh"
      if [ "$has_third" = true ]; then
        konsole --new-tab -e zsh -c "cd '$current_dir' && claude3; exec zsh"
      fi
    elif command -v xterm &> /dev/null; then
      xterm -e zsh -c "cd '$current_dir' && claude; exec zsh" &
      xterm -e zsh -c "cd '$current_dir' && claude2; exec zsh" &
      if [ "$has_third" = true ]; then
        xterm -e zsh -c "cd '$current_dir' && claude3; exec zsh" &
      fi
    else
      echo "   Tab 1: cd '$current_dir' && claude"
      echo "   Tab 2: cd '$current_dir' && claude2"
      if [ "$has_third" = true ]; then
        echo "   Tab 3: cd '$current_dir' && claude3"
      fi
    fi
  fi
}

# Open Claude Code sessions in danger mode (accounts 1–3 when configured)
claudes-danger() {
  local current_dir=$(pwd)
  local has_third=false
  command -v claude3-danger &>/dev/null && has_third=true

  if [[ "$TERM_PROGRAM" == "iTerm.app" ]] || [[ -n "$ITERM_SESSION_ID" ]]; then
    if command -v osascript &> /dev/null; then
      if [ "$has_third" = true ]; then
        osascript <<EOF
tell application "iTerm"
  tell current window
    create tab with default profile
    tell current session of current tab
      write text "cd '$current_dir' && echo '🔥 Claude conta 1 (danger)' && claude1-danger"
    end tell
    create tab with default profile
    tell current session of current tab
      write text "cd '$current_dir' && echo '🔥 Claude conta 2 (danger)' && claude2-danger"
    end tell
    create tab with default profile
    tell current session of current tab
      write text "cd '$current_dir' && echo '🔥 Claude conta 3 (danger)' && claude3-danger"
    end tell
  end tell
end tell
EOF
      else
        osascript <<EOF
tell application "iTerm"
  tell current window
    create tab with default profile
    tell current session of current tab
      write text "cd '$current_dir' && echo '🔥 Claude conta 1 (danger)' && claude1-danger"
    end tell
    create tab with default profile
    tell current session of current tab
      write text "cd '$current_dir' && echo '🔥 Claude conta 2 (danger)' && claude2-danger"
    end tell
  end tell
end tell
EOF
      fi
    fi
  elif [[ "$TERM_PROGRAM" == "Apple_Terminal" ]]; then
    if command -v osascript &> /dev/null; then
      if [ "$has_third" = true ]; then
        osascript <<EOF
tell application "Terminal"
  do script "cd '$current_dir' && echo '🔥 Claude conta 1 (danger)' && claude1-danger"
  tell application "System Events" to keystroke "t" using command down
  do script "cd '$current_dir' && echo '🔥 Claude conta 2 (danger)' && claude2-danger" in front window
  tell application "System Events" to keystroke "t" using command down
  do script "cd '$current_dir' && echo '🔥 Claude conta 3 (danger)' && claude3-danger" in front window
end tell
EOF
      else
        osascript <<EOF
tell application "Terminal"
  do script "cd '$current_dir' && echo '🔥 Claude conta 1 (danger)' && claude1-danger"
  tell application "System Events" to keystroke "t" using command down
  do script "cd '$current_dir' && echo '🔥 Claude conta 2 (danger)' && claude2-danger" in front window
end tell
EOF
      fi
    fi
  else
    echo "Opening Claude accounts (danger) in: $current_dir"
    if command -v gnome-terminal &> /dev/null; then
      if [ "$has_third" = true ]; then
        gnome-terminal --tab --title="Claude 1 danger" -- bash -c "cd '$current_dir' && claude1-danger; exec zsh" \
                       --tab --title="Claude 2 danger" -- bash -c "cd '$current_dir' && claude2-danger; exec zsh" \
                       --tab --title="Claude 3 danger" -- bash -c "cd '$current_dir' && claude3-danger; exec zsh"
      else
        gnome-terminal --tab --title="Claude 1 danger" -- bash -c "cd '$current_dir' && claude1-danger; exec zsh" \
                       --tab --title="Claude 2 danger" -- bash -c "cd '$current_dir' && claude2-danger; exec zsh"
      fi
    elif command -v konsole &> /dev/null; then
      konsole --new-tab -e zsh -c "cd '$current_dir' && claude1-danger; exec zsh" \
              --new-tab -e zsh -c "cd '$current_dir' && claude2-danger; exec zsh"
      if [ "$has_third" = true ]; then
        konsole --new-tab -e zsh -c "cd '$current_dir' && claude3-danger; exec zsh"
      fi
    elif command -v xterm &> /dev/null; then
      xterm -e zsh -c "cd '$current_dir' && claude1-danger; exec zsh" &
      xterm -e zsh -c "cd '$current_dir' && claude2-danger; exec zsh" &
      if [ "$has_third" = true ]; then
        xterm -e zsh -c "cd '$current_dir' && claude3-danger; exec zsh" &
      fi
    else
      echo "   Tab 1: cd '$current_dir' && claude1-danger"
      echo "   Tab 2: cd '$current_dir' && claude2-danger"
      if [ "$has_third" = true ]; then
        echo "   Tab 3: cd '$current_dir' && claude3-danger"
      fi
    fi
  fi
}

### -------------------------------
### HISTORY (reaproveitar comandos entre sessões)
### -------------------------------
HISTFILE=~/.zsh_history
HISTSIZE=50000
SAVEHIST=50000

setopt APPEND_HISTORY
setopt SHARE_HISTORY
setopt HIST_IGNORE_DUPS
setopt HIST_REDUCE_BLANKS

### -------------------------------
### QUALITY OF LIFE
### -------------------------------
setopt autocd
setopt interactive_comments
setopt correct

### -------------------------------
### LOAD SYNTAX HIGHLIGHTING LAST
### -------------------------------
zinit light $SYNTAX_PLUGIN

### -------------------------------
### NVM (Node Version Manager)
### -------------------------------
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"

### -------------------------------
### STARSHIP (prompt)
### -------------------------------
unalias zi 2>/dev/null
eval "$(starship init zsh)"
alias zi='zinit' 2>/dev/null
